# simulations/network/dns_timeout.yaml
# Smoke test: DNS resolver timeout (exercises framework timeout handling)
suite: network
emulates: dns_enumeration
target: "slow-resolver.example.local"
disposition: error

behavior:
  failure_mode: timeout

output: {}
