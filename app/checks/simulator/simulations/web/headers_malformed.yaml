# simulations/web/headers_malformed.yaml
# Smoke test: malformed output for framework validation testing
suite: web
emulates: header_analysis
target: "buggy.example.local"
disposition: error

behavior:
  failure_mode: malformed

output:
  headers: "this should be a dict not a string"
  status_code: "two hundred"
